[Unit]
Description=TRMNL Weather Plugin
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/trmnl-weather
Environment=PATH=/usr/bin:/usr/local/bin
ExecStart=/usr/bin/python3 /opt/trmnl-weather/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
